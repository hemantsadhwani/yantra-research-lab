# Seed papers — initial knowledge-base backfill (50)

The curated quant reading-list (Black-Scholes → Monte Carlo → Heston/SABR/rough-vol →
Almgren-Chriss → Markowitz/Kelly → stat-arb → López de Prado backtest-overfitting), tagged
`📐📊🖼` so the formula/table/diagram-heavy ones double as ingestion-pipeline stress tests.

**Canonical copy:** `../../job_profile/ai_enginerring/QUANT_PAPERS_SEEDLIST.md`
(copied into this repo when the `ingestion/` tier is built, so the corpus is self-contained).
</content>
