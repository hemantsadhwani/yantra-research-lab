# Tier-1 needs NONE of these — it runs on the standard library.
# These are for Tier-2/3 services as they come online.

# --- LLM / model gateway (Tier-2 proposer, LLM-as-judge, chatbot) ---
ANTHROPIC_API_KEY=
# LITELLM_MODEL=claude-sonnet-4-6

# --- embeddings + vector DB (chatbot / ingestion) ---
VOYAGE_API_KEY=
# QDRANT_URL=

# --- observability (ops) ---
LANGSMITH_API_KEY=
LOGFIRE_TOKEN=

# --- AWS (infra / deploy) ---
AWS_REGION=ap-south-1
