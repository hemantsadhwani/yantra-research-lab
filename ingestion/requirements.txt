# Tier-3 agentic ingestion pipeline — SEPARATE from the serving container (backend/).
# This is a batch/offline job; none of these heavy parsers ship in the API image.

langgraph>=0.2,<0.4            # StateGraph orchestration: retries, checkpoints, HITL
langchain-core>=0.3,<0.4      # graph primitives langgraph builds on
pymupdf>=1.24,<1.29           # PDF text + tables (find_tables) + images, one lib, no torch
pytesseract>=0.3,<0.4         # OCR fallback for image-only pages (needs `tesseract` binary)
beautifulsoup4>=4.12,<5       # HTML parsing (future web sources)
lxml>=5,<7                    # fast HTML/XML backend for bs4 + arXiv Atom
httpx>=0.27,<0.29             # fetch (with retries/backoff)
Pillow>=10,<12                # image handling for OCR (kept <12 to match fastembed)
boto3>=1.34,<2                # S3 object storage (bronze raw PDFs + images)

# Shared with serving (kept version-compatible so embeddings match at query time):
qdrant-client>=1.9,<1.13
fastembed>=0.3,<0.8           # BAAI/bge-small-en-v1.5 — MUST match backend/embeddings.py
anthropic>=0.49,<1.0          # enrichment (Haiku) + phase-2 vision captions
python-dotenv>=1.0,<2.0
logfire[fastapi]>=4,<5        # pipeline observability (same project as serving)
pydantic>=2.7,<3.0            # data contracts between stages
