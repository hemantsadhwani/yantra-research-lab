export default function Footer() {
  return (
    <footer className="sitefooter">
      <p>
        <strong>yantra-research-lab</strong> — an autonomous strategy-research showcase.{" "}
        <code>propose → backtest → judge → rank → remember</code>, bounded by a budget, with a
        human approval gate before anything is promoted.
      </p>
      <p style={{ marginTop: 10 }}>
        ALL PAPER / SIMULATED — educational, not investment advice. No real funds are held, moved,
        or managed. Every number shown across this site is a placeholder or a simulated result.
      </p>
    </footer>
  );
}
